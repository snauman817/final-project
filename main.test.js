const {test, basicShot, hardShot, rareShot,targetBasic,tCoordinates} = require('./main');

describe('shot functions', () => {
  test('basic item disapears', () => {
   expect(click, targetBasic.style.display).toBe("none")
});
});
